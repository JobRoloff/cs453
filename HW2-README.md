## Assignment

create a main file called Typecheck.java 
generate some files from jtb and jcc to help generate some basic files for the project
our assignment submission is only concerned with the 2 files we are to create ourselves:

- Typecheck.java
- 

## Relevant files

| Name               | Purpose                                                         |
| ------------------ | --------------------------------------------------------------- |
| Typecheck.java     | 1. pretty print debug 2. Building symbol table in 2nd tree walk |
| visitor/GJDepthFirst.java | Node specific treatment                                         |

## Concepts

A pretty printer walks parse tree and prints to the terminal such that deeper levels of the parse tree is indented more than ancestor nodes.

the ast class generated by javacc will be the non terminal names of our grammar. What this means is that we'll be expecting a node subtype class for each terminal of our grammar. 

## Questions: 

- what's the role of SymTableVis? && where do i import if from?
- what's a summary grammar? asking becausee its mentioned in 2-17 lec @ 18:07 when discussing syntaxtree/ node types
- how do we implement a type checker for all tthe type files auto generated within the syntaxtre/ directory? see "primary plus expression" - hint use inheritance. 
- What is the visitor system implementation introduced in 1-17 lev @24:30?
	- when we implement the postfix ast walk, we need generic node type support. 

## Phases in type checking

// note Typecheck.java handles the following

1. Debugging with pretty print
2. Build a symbol table thatt visits the ast and then collects info on every declaration - meaning the following - 
	* for whatever code the parser is looking at in the ast, we determine the data type the code is. with that info, we "register" the type in which that code represents. This stored type information is then used when we walk the ast a second time such that on the second walk, we propagate the type back up the tree?
	
	on the second traversal, we compute type information and propagate type errors upwards


## Notes

### Javacc

javacc also provides a class called MiniJavaParser. this class has a variable called root which represents a tree data structure - the ast of the program

javacc provides an auto generated class called root with an instance method on the called .accept(). With .accpet(), we use it to 

### /syntaxtree

this dir contains one file per type thatt could show up in the ast

nodechoice along with the various nodelist java files are used for handling the situation where I need to represent constructs added to the grammar. The concept of constructs are like when you need to do things such as grouping things in a set a parenthesis. 

0+ is node list
1+ is a node choice